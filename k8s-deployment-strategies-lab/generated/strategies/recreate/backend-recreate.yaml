apiVersion: apps/v1
kind: Deployment
metadata:
  name: springboot-recreate
  namespace: bank-app
spec:
  replicas: 2
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: springboot-recreate
  template:
    metadata:
      labels:
        app: springboot-recreate
    spec:
      containers:
        - name: springboot
          image: ratana/springboot:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 9024
          envFrom:
            - configMapRef:
                name: springboot-config
            - secretRef:
                name: springboot-secret
          env:
            - name: RELEASE_VERSION
              value: "v1"
---
apiVersion: v1
kind: Service
metadata:
  name: springboot-recreate-service
  namespace: bank-app
spec:
  selector:
    app: springboot-recreate
  ports:
    - port: 9024
      targetPort: 9024
