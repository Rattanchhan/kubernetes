apiVersion: apps/v1
kind: Deployment
metadata:
  name: react-stable
  namespace: bank-app
spec:
  replicas: 4
  selector:
    matchLabels:
      app: react-canary
      track: stable
  template:
    metadata:
      labels:
        app: react-canary
        track: stable
    spec:
      containers:
        - name: react
          image: ratana/reactjs:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
          env:
            - name: RELEASE_VERSION
              value: "stable"
